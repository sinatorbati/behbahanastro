<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || 'پنل مدیریت' %></title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    body { font-family: system-ui, Arial, sans-serif; max-width: 980px; margin: 2rem auto; padding: 0 1rem; direction: rtl; }
    header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1.5rem; }
    nav a { margin-left: 1rem; margin-right: 0; }
    table { width: 100%; border-collapse: collapse; }
    th, td { border-bottom: 1px solid #ddd; padding: 8px; text-align: left; }
    .actions { display: flex; gap: .5rem; }
    form.inline { display: inline; }
    .error { color: #b00; margin: .5rem 0; }
    input[type=text], textarea { width: 100%; padding: .5rem; }
    input[type=file] { margin-top: .25rem; }
    .btn { background: #4f46e5; color: #fff; border: 0; padding: .55rem .9rem; cursor: pointer; border-radius: 8px; text-decoration: none; display: inline-block; box-shadow: 0 6px 16px rgba(79,70,229,.2); }
    .btn.secondary { background: #64748b; }
    .btn.danger { background: #ef4444; }
    .img-preview { max-height: 100px; border:1px solid #e5e7eb; border-radius:8px; }
    main.card { background:#fff; border:1px solid #e5e7eb; border-radius:12px; padding:16px; box-shadow:0 10px 30px rgba(0,0,0,0.06); }
  </style>
</head>
<body>
  <header>
    <h1>پنل مدیریت انجمن</h1>
    <nav>
      <a href="/admin" class="btn secondary">پیشخوان</a>
      <a href="/admin/articles/new" class="btn">مقاله جدید</a>
      <a href="/admin/logout" class="btn danger">خروج</a>
    </nav>
  </header>
  <main class="card">
    <%- body %>
  </main>
</body>
</html>


